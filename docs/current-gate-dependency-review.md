# Current Gate Dependency Review

Status: **APPROVED FOR EXACT-REVISION QUALIFICATION.**

This successor introduces no new third-party package, donor code, model, binary, external service, persisted storage location, network surface, platform API, or customer filesystem decision. It binds the accepted project-owned source-free reusable view catalog into the accepted bounded operator-control lifecycle.

Catalog installation consumes only the already-accepted canonical source-free catalog payload and validates the complete candidate before replacing the active in-memory catalog. Every installed view must preserve the active logical-slot set; application routes the selected catalog layout through the existing bounded relayout queue, preserving arbitrary x/y/width/height/z geometry, sparse logical slots through 4095, media/source bindings, selection semantics, deterministic stack ordering, and presentation generation. Malformed/oversized input, incompatible layouts, missing/invalid view identifiers, invalid session state, and saturated control queues fail closed without partial catalog or active-layout mutation.

Retained observability adds only installed catalog view count and aggregate successful install/apply counters around the accepted privacy-safe operator snapshot. Camera/source identity, credentials, RTSP/network data, payload/media data, recording identity, private paths, native handles/pointer values, runner identity, analytics, source assignment, and automatic persistence remain excluded.

No new third-party license or distribution obligation is introduced. Existing dependency and provenance controls remain governing.

This is an engineering dependency review, not legal advice.
